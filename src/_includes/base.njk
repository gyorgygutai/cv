<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <meta property="og:type" content="website">
  
  <title>{{ profile.name }}</title>
  <meta name="description" content="{{ profile.description }}">
  <meta property="og:title" content="{{ profile.name }}">
  <meta property="og:description" content="{{ profile.description }}">
  <meta property="og:site_name" content="{{ profile.website }}">
  <meta property="og:image" content="{{ profile.shareImage }}">
  
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{ profile.name }}">
  <meta name="twitter:description" content="{{ profile.description }}">
  <meta name="theme-color" content="#222">
  
  <link rel="preconnect" href="https://{{ profile.website }}">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-white dark:bg-[#222] text-[#111] dark:text-[#eee] text-[14px] leading-[1.6] font-sans antialiased">
  <div class="opacity-100">
    <div class="opacity-100">
      <main class="min-h-screen">
        <div class="max-w-[672px] mx-auto px-6 py-12 md:px-[72px] md:py-[72px] opacity-100">
          {{ content | safe }}
        </div>
      </main>
    </div>
  </div>
</body>
</html>
